EnumerableQuery
    .SelectMany(
        src => (src.Manager != null)
            ? src.Manager.Projects ?? Enumerable.Empty()
            : Enumerable.Empty())
    .GroupBy(src => new AnonymousType(src.Name))
    .Select(g => new AnonymousType(g.Key, g.Count()))