[Source]
    .GroupBy(src => new AnonymousType(src.Company.Name))
    .Select(
        g => new AnonymousType(
            g.Key,
            g.Count(),
            g.Min(e => e.Seniority),
            g.Max(e => e.Seniority)))
    .Where(row => row.Item4 == p)